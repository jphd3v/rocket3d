Drop tracker module files such as `.xm`, `.mod`, `.s3m`, or `.it` in this directory.

Music tracks in this folder are discovered automatically. `M` cycles through them in-game, with `OFF` as the first option.
